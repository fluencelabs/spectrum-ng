apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - router.yml
  - groups.yml
  - policy.yml
  # One-off cleanup of an orphaned cluster-wide secret-read binding; see the file header.
  - support-rbac-cleanup.yml
