apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
  # EXPERIMENT, stage only — see newapi-probe.yml. Remove together with that file.
  - newapi-probe.yml
patches:
  - path: router-patch.yml
