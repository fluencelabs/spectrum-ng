apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ks.yml
  # Mesh exposure Flux Kustomization (NBResource for k8s.<id>.<net>.spectrum). Co-located here — and
  # its spec.path still points at netbird-services/app/kube-oidc-proxy — so the whole kube-oidc-proxy
  # feature is gated by a single dir include in clusters/stage (stage-only).
  - netbird-ks.yml
