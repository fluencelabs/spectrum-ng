apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: fluence
resources:
  - serviceaccount.yml
  - clusterrole.yml
  - clusterrolebinding.yml
  - pvc.yml
  - deployment.yml

configMapGenerator:
  - name: controller-config
    envs:
      - config.env
