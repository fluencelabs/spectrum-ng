apiVersion: v1
kind: Service
metadata:
  name: admission-controller
spec:
  type: ClusterIP
  selector:
    app: admission-controller
  ports:
    - name: https
      port: 443
      targetPort: 8443
      protocol: TCP
