apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: admission
  labels:
    app.kubernetes.io/name: admission
rules:
  - apiGroups: ["*"]
    resources: ["*"]
    verbs: ["*"]
